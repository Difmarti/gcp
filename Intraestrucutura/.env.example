# ==============================================================================
# Variables de Entorno para Gitea Terraform Runners
# ==============================================================================
# Copia este archivo a .env y ajusta los valores
# ==============================================================================

# URL del servidor Gitea
GITEA_INSTANCE_URL=http://10.30.90.102:3000

# Token de registro del runner (obtener desde Gitea Admin → Actions → Runners)
GITEA_RUNNER_TOKEN=tu-token-de-registro-aqui

# Nombres de los runners (opcionales, se generan automáticamente si no se especifican)
RUNNER_NAME_1=gitea-terraform-runner-1
RUNNER_NAME_2=gitea-terraform-runner-2
RUNNER_NAME_3=gitea-terraform-runner-3

# Labels del runner - IMPORTANTE: debe apuntar al nombre de la imagen correcta
# Formato: label1:docker://imagen:tag,label2:docker://imagen:tag
RUNNER_LABELS=ubuntu-latest:docker://gitea-terraform-runner:latest,terraform:docker://gitea-terraform-runner:latest

# ==============================================================================
# NOTAS:
# ==============================================================================
#
# 1. El RUNNER_LABELS debe coincidir con el nombre de la imagen construida
# 2. Los workflows que usen runs-on: ubuntu-latest usarán esta imagen
# 3. Puedes agregar más labels separados por comas
# 4. Ejemplo de labels adicionales:
#    RUNNER_LABELS=ubuntu-latest:docker://gitea-terraform-runner:latest,terraform:docker://gitea-terraform-runner:latest,gcp:docker://gitea-terraform-runner:latest
#
